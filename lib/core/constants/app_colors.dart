import 'package:flutter/material.dart';

/// King Domain brand v0 — see king-domain-mobile/CLAUDE.md.
/// Not locked; a motion designer may revise it later. Use these values
/// rather than inventing new ones, so a future change is a single edit here.
class AppColors {
  AppColors._();

  static const Color ink = Color(0xFF151A2E);
  static const Color ink2 = Color(0xFF1E2438);
  static const Color ink3 = Color(0xFF2A3149);
  static const Color paper = Color(0xFFFAF8F3);
  static const Color paper2 = Color(0xFFF1EEE6);

  static const Color gold = Color(0xFFC9A227);
  static const Color goldSoft = Color(0xFFE0BE4A);

  static const Color signalGreen = Color(0xFF2F7A5E);
  static const Color greenSoft = Color(0xFF4FA37F);

  static const Color slate = Color(0xFF5B6178);
  static const Color slateDim = Color(0xFF8A90A3);

  /// Status badges only — never general decoration.
  static const Color openPending = Color(0xFFE2896C);
  static const Color settled = Color(0xFF6FBBA2);
}
